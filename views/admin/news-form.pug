extends ../layout

block content
  h1= title

  if errors
    each error in errors
      p=error
      
  form(method='post')
    p
      label Tytuł
      input(type='text' name='title' value=(body && body.title) ? body.title : '')
    p
      label Opis
      textarea(name='description')= body && body.description ? body.description : ''
    p
      input(type='submit' value='Zapisz')

